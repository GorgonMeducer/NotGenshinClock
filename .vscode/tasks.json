{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Make",
            "group": "build",
            "type": "shell",
            "command": "make -j",
            "options": {
                "cwd": "${workspaceFolder}"
            }
        },
        {
            "label": "Make Clean",
            "group": "build",
            "type": "shell",
            "command": "make clean",
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ]
}